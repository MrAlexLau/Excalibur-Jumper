// configuration

var Config = {
   gameHeight: 480,
   gameWidth: 320,
   groundHeight: 130,
   goalWidth: 20,
   goalHeight: 20,
   dx: 200,
   jumperHeight: 50,
   jumperWidth: 35,
   soundVolume: .1,
   pressToStart: 'Press Space to Start' // 182px in width
}

// calculated values
Config.startLabelX = Config.gameWidth / 2 - 91;
